/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ['./src/**/*.{js,jsx,ts,tsx}'],
  theme: {
    extend: {},
  },
  plugins: [],
};

// 테일윈드 번들링 (vue파일을 사용하고 싶으면 content안에 vue를 넣으면됨)
