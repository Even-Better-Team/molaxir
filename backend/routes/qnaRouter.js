// import express from "express";
// import * as qnaController from "../controllers/qnaController.js";

// const router = express.Router();

// router.post("/", qnaController.createPost);
// router.delete("/:qnaId", qnaController.deletePost);
// router.patch("/:qnaId", qnaController.updatePost);
// router.get("/", qnaController.getPost);
// router.get("/:qnaId", qnaController.getPostDetail);

// export default router;
